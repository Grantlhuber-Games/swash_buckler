/** mongoose like model definition **/

const
  model = {
    name: "Avatar",
    modelSchema: {
        //keySchema: {},
        schema: {
            spawned: "bool"
        }
    }
}

export default model;