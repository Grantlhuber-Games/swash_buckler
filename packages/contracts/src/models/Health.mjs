/** mongoose like model definition **/
const model = {
    name: "Health",
    modelSchema: {
        // keySchema: {},
        schema: {
            health: "uint32"
        }
    }
}

export default model;